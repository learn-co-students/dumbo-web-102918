<h1>Index of statuses</h1>
<%= link_to "New status", new_status_path %>
<ol>
<% @statuses.each do |status| %>
  <li>
    <%= link_to status.body, status %>
    <%= status.reactions.map(&:emoji).join("") %>
  </li>
<% end %>
</ol>